[project]
name = "st-rsuite"
version = "0.7.0"

# One component (one asset_dir, one bundle) serves every widget; the frontend
# routes on the `kind` discriminator in `data`. See st_rsuite/_component.py.
[[tool.streamlit.component.components]]
name = "rsuite"
asset_dir = "frontend/build"
